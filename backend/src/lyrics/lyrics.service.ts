import { Injectable } from '@nestjs/common';

@Injectable()
export class LyricsService {
  search(searchDto: any) {
    // Placeholder implementation - will be expanded in later tasks
    return { lyrics: '', source: '', confidence: 0 };
  }
}